import React from 'react';
import { AppScreen } from '@/components/ui';
import { LiteratureReader } from '@/features/resources/components/LiteratureReader';

export default function LiteratureScreen() {
  return (
    <AppScreen>
      <LiteratureReader />
    </AppScreen>
  );
}
