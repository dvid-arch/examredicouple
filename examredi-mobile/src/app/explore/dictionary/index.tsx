import React from 'react';
import { AppScreen } from '@/components/ui';
import { Dictionary } from '@/features/resources/components/Dictionary';

export default function DictionaryScreen() {
  return (
    <AppScreen>
      <Dictionary />
    </AppScreen>
  );
}
