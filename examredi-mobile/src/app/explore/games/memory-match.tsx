import React from 'react';
import { AppScreen } from '@/components/ui';
import { MemoryMatch } from '@/features/games/components/MemoryMatch';

export default function MemoryMatchScreen() {
  return (
    <AppScreen>
      <MemoryMatch />
    </AppScreen>
  );
}
